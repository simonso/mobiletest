apply plugin: 'android'

println(project.property("suplean.signing") + ".gradle");

if (project.hasProperty("suplean.signing") 
      && new File(project.property("suplean.signing") + ".gradle").exists()) {
       apply from: project.property("suplean.signing") + ".gradle";
}


android {

    compileSdkVersion 18
    buildToolsVersion "17.0.0"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 16
    }
}

dependencies {
//	compile 'com.android.support:support-v4:18.0.0'
}
